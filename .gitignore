node_modules/
dist/
feature/generated/recorded-session.json
test-results/
playwright-report/
.env
.env.local
# config.yaml contains the Ollama API key — keep it out of version control.
# LLM credentials for the AI Scenario CLI (npm run ai)
e2e/config/llm.json
